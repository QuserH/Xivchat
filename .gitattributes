* text eol=lf
*.ttf binary
*.png binary
*.ico binary
*.dll binary
# 下面这些是补的。第一行 `* text eol=lf` 把**所有**文件当文本做行尾转换，
# 二进制文件必须逐个标出来豁免，否则内容里每个 0D0A 都会被吃成 0A。
# 已经吃出来过两次：
#   assets/wiki/items.db.gz  —— 提交前抓到（gzip -t 报 CRC 错，会让 wiki 库解不开）
#   wallpaper_dusk_dark.jpg  —— 已经坏在仓库里了（86992 → 86987，丢 5 字节）
# .webp 那两个是侥幸：内容里恰好没有 0D0A 字节对。
*.gz binary
*.zip binary
*.db binary
*.jpg binary
*.jpeg binary
*.webp binary
*.so binary
*.jar binary
*.apk binary
*.keystore binary
*.jks binary
